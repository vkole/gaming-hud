{
  "name": "gaming-moap-hud",
  "version": "1.0.0",
  "description": "Gaming MOAP HUD for Second Life",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": "22.x"
  }
}
