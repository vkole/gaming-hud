# Environment configuration for local development
# For production on Render, use environment variables in the dashboard

# Database connection string (Render PostgreSQL)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/gaming_hud

# Node environment
NODE_ENV=development

# Server port
PORT=3000
